# GEMINI_API_KEY: Optional. Enables the AI Door Specification assistant (/api/door-assistant).
# If unset, the assistant returns a built-in advisory fallback and the rest of the app works normally.
# On Vercel: add this under Project -> Settings -> Environment Variables.
GEMINI_API_KEY=""
